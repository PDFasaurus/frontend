import { environment } from '../environments/environment';

export const AUTH_COOKIE: string = environment.AUTH_COOKIE;
export const API_PATH: string = environment.API_PATH;
export const PAGE_SIZE: number = environment.PAGE_SIZE;
export const API_CALL_INCREMENT: number = environment.API_CALL_INCREMENT;
export const DOLLAR_FOSSIL_VALUE: number = environment.DOLLAR_FOSSIL_VALUE;
export const API_CALL_PRICING_INCREMENT: number = environment.API_CALL_PRICING_INCREMENT;
export const PRODUCT_ID: number = environment.PRODUCT_ID;
export const VENDOR_ID: number = environment.VENDOR_ID;

export const ERRORS: any = {
  GENERIC: 'Something has gone wrong',
}
